name: Cache Workflow

on:
  push:
  workflow_dispatch:

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/demo
          key: cache-5234b8f

      - name: prime-cache-5234b8f
        run: echo "Cache hit? ${{ steps.cache.outputs.cache-hit }}"
